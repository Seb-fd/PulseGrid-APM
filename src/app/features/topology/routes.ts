import type { Routes } from '@angular/router';
import { TopologyMapComponent } from './topology-map.component';

export const TOPOLOGY_ROUTES: Routes = [{ path: '', component: TopologyMapComponent }];

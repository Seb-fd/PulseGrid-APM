import type { Routes } from '@angular/router';
import { DashboardGridComponent } from './dashboard-grid.component';

export const DASHBOARD_ROUTES: Routes = [{ path: '', component: DashboardGridComponent }];

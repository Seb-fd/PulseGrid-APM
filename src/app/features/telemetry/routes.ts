import type { Routes } from '@angular/router';
import { TelemetryPageComponent } from './telemetry-page.component';

export const TELEMETRY_ROUTES: Routes = [{ path: '', component: TelemetryPageComponent }];
